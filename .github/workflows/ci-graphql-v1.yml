name: CI(graphql-v1)

on:
  push:
    branches:
      - main
  pull_request:
    paths:
      - .github/workflows/ci-graphql-v1-services.yml
      - graphql-services/v1/**

defaults:
  run:
    working-directory: graphql-services/v1

jobs:
  eslint:
    name: ESLint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: bahmutov/npm-install@v1
        with:
          working-directory: graphql-services/v1
      - run: yarn run lint:eslint

  typecheck:
    name: Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: bahmutov/npm-install@v1
        with:
          working-directory: graphql-services/v1
      - run: yarn run generate
      - run: yarn run typecheck

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: bahmutov/npm-install@v1
        with:
          working-directory: graphql-services/v1
      - run: yarn run generate
      - run: yarn run build
