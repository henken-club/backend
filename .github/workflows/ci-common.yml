name: CI(common)

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  dprint:
    name: dprint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: dprint/check@v2.0
